#!/usr/bin/env bash
#Making a while loop that displays 12 hours and 59 minutes.

x=0
i=1
while ((x <= 12))
      do
	  echo "$x"
	  x=$(( x + 1 ))
	  while ((i <= 59))
	  do
	      echo "$i"
	      i=$(( i + 1 ))
	  done
